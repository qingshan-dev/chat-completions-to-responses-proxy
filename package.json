{
  "name": "chat-completions-to-responses-proxy",
  "version": "0.1.0",
  "description": "Local compatibility proxy that exposes /v1/chat/completions and forwards to a Responses upstream.",
  "main": "chat-completions-to-responses-proxy.js",
  "bin": {
    "chat-completions-to-responses-proxy": "./chat-completions-to-responses-proxy.js"
  },
  "scripts": {
    "check": "node --check chat-completions-to-responses-proxy.js && node --check test-chat-proxy.js",
    "test": "node test-chat-proxy.js"
  },
  "keywords": [
    "openai",
    "chat-completions",
    "responses-api",
    "proxy"
  ],
  "license": "MIT",
  "type": "commonjs"
}
